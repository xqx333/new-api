# 本地定制补丁说明

本文件记录本分支相对上游 `upstream/main` 的本地定制功能。后续同步上游时，应先保留上游完整实现，再逐项确认本文件中的功能是否仍需补丁；如果上游已经实现等价语义，优先采用上游实现并删除重复补丁。

## 图片 URL 转 base64

- 环境变量：`IMAGE_TO_BASE64`
- 默认值：`true`
- 作用：在文本请求转发前，将请求内容中的远程图片 URL 转为 `data:image/*;base64,...`。
- 覆盖入口：
  - `chat/completions` 兼容入口
  - `responses` 入口
- 实现原则：
  - 复用统一文件服务 `types.FileSource` / `service.GetBase64Data`。
  - 只处理 `http://` 和 `https://` 图片 URL。
  - 跳过空 URL、非远程 URL 和已有 `data:` URL。
  - 单个图片转换失败时不阻断请求，保留原始请求继续转发。

## 指定渠道跳过 token 统计

- 环境变量：`SKIP_TOKEN_CHANNEL_IDS`
- 默认值：空
- 示例：`SKIP_TOKEN_CHANNEL_IDS=233,214`
- 作用：指定渠道 ID 命中后，跳过本地 token 统计，避免本地反复读取媒体或请求远程文件。
- 实现原则：
  - 启动时解析一次环境变量。
  - 非法 ID 只记录日志并忽略。
  - 命中渠道时在 token 估算入口提前返回固定预估值。

## 渠道测试默认文案

- 作用：渠道测试请求中的默认用户消息从 `hi` 改为 `bye`。
- 覆盖范围：
  - OpenAI chat / Anthropic / Gemini 测试请求
  - OpenAI responses 测试请求
  - responses compact 测试请求
- 不改变 embedding、rerank、image generation 的测试样例。

## 用户与模型精确限流

- 系统设置：`ModelRequestRateLimitUserModel`
- 隐藏限流详情：`ModelRequestRateLimitHideDetailsEnabled`，默认 `false`。
- 配置格式：JSON 数组，每项包含 `user_id`、`model`、`max_requests` 和 `max_success`。
- 作用：只对指定用户请求指定原始模型时应用独立速率限制，并对该用户的所有 API 密钥共享计数。
- 行为：
  - 沿用模型请求限流总开关和统一时间窗口。
  - 优先级为“用户模型规则 > 用户分组规则 > 全局规则”。
  - 模型按渠道映射前的客户端请求名称精确、区分大小写匹配，不支持通配符。
  - `max_requests=0` 或 `max_success=0` 分别表示该项不限；专属规则不允许两项同时为 `0`。
  - 全局或分组的两项限制均为 `0` 时直接放行，不创建 Redis 或内存计数。
  - 开启“隐藏限流详情”后，拒绝请求时只返回通用 OpenAI 格式 `429`，不包含限额、时间窗口、用户或模型信息；关闭时保留上游原有响应。
  - 隐藏后的错误固定为 `type=requests`、`code=rate_limit_exceeded`，提示为 `Rate limit reached for requests. Please try again later.`。
- 性能原则：
  - 只有当前用户至少配置了一条专属规则时才提前解析模型。
  - 模型解析结果由限流和渠道分发复用，避免重复读取请求体。
  - 未配置专属规则的用户继续走原有限流路径，不增加请求体解析开销。
- 部署说明：未启用 Redis 时计数只在当前进程内有效，多实例部署需要 Redis 才能共享限额。
- 更新原则：如果上游以后实现同等粒度和语义的用户模型限流，优先采用上游实现并删除本地重复补丁。

## 自定义补全倍率优先

- 作用：`CompletionRatio` JSON 中的显式配置优先于后端硬编码默认倍率。
- 行为：
  - 如果模型名在 `CompletionRatio` 中显式配置，计费时使用该值。
  - 如果没有显式配置，才回退到上游/后端默认倍率。
  - 前端说明文案明确显式配置会覆盖后端默认值。

## 价格编辑器适配原则

- 当前前端目录：`web`
- 主要目的：配合“自定义补全倍率优先”语义，避免可视化编辑时误删、覆盖或隐藏用户显式配置。
- 当前上游编辑器已经满足所需语义，因此不额外修改编辑器逻辑：
  - 修改输入价格时保留已填写的补全、缓存、图片和音频价格，只重新计算对应倍率。
  - 清空或关闭可选价格字段时，保存结果不再包含对应 JSON 配置。
  - 手动填写补全价格时，即使换算倍率等于后端默认值，也会显式保存。
- 上述行为用于说明当前实现如何满足目的，不作为以后必须照搬的固定实现。后续同步应根据上游最新编辑器重新判断，只要显式 `CompletionRatio` 不被误删或默认值覆盖即可。
